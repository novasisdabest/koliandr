name: Deploy zeeko.cz

on:
  push:
    branches: [main]
    paths:
      - 'zeeko.cz/**'
      - '.github/workflows/deploy-zeeko.yml'
  workflow_dispatch:

# Dva deploye stejného webu naráz by si navzájem přepsaly FTP stav.
concurrency:
  group: deploy-zeeko
  cancel-in-progress: false

defaults:
  run:
    working-directory: zeeko.cz

jobs:
  deploy:
    name: Build & upload
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: zeeko.cz/package-lock.json

      - name: Install
        run: npm ci

      - name: Build (astro check + astro build)
        run: npm run build

      - name: Guard — v dist nesmí zůstat žádný externí požadavek
        run: |
          if grep -rEo 'https?://[^"'"'"' )]+' dist/*.html \
             | grep -vE 'schema\.org|zeeko.cz|wa\.me|instagram\.com|zeeko\.cz|parkovistenemovitosti\.cz|w3\.org'; then
            echo "::error::Build obsahuje externí zdroj — poruší CSP v .htaccess."
            exit 1
          fi
          echo "OK: žádné externí zdroje."

      - name: Upload via FTPS
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.ZEEKO_FTP_HOST }}
          username: ${{ secrets.ZEEKO_FTP_USER }}
          password: ${{ secrets.ZEEKO_FTP_PASSWORD }}
          protocol: ftps
          port: 21
          local-dir: ./zeeko.cz/dist/
          server-dir: ${{ secrets.ZEEKO_FTP_REMOTE_DIR }}
          # NIKDY nezapínat: smaže celý docroot včetně věcí, co tam nahrál někdo ručně.
          dangerous-clean-slate: false
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
